import React from 'react';
import { Container, Typography, Box } from '@material-ui/core';

export default function JobOpportunities(props) {
    return (
        <>
            <Box m={20}/>
            <Container>
                <Typography variant='h2'>Vagas abertas</Typography>
            </Container>
        </>
    );
};